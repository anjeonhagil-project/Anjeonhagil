// 구현 예정(Anjeonhagil): 경로 검색 요청·로딩·오류·재시도·이전 요청 취소를 RouteComparePage에서 분리한다.
// 입력: OD, 명시적 출발시각, 이번 검색의 Q3. 출력: 서버가 저장한 search_id와 후보 목록, 진행/오류 상태.
// Q3 null 프로필은 DetourToleranceSheet로 이번 값을 선택한 후 검색한다. 좌표/허용시간 변경 시 이전 응답을 표시하지 않는다.
// API는 기존 routes/api.js를 사용한다. 프로필·모델 버전·피처는 서버가 결정하며 클라이언트에서 재계산하지 않는다.
// 이 훅은 후보를 받은 사실만 관리한다. 노출은 useRouteExposure, 최종 선택은 명시적 사용자 행동으로 분리한다.
