# 수정 필요(Anjeonhagil): 구현 시 라우팅 설정과 MODEL_WORKER_URL/MODEL_WORKER_TOKEN/MODEL_WORKER_TIMEOUT_MS 예시를 추가한다. 비밀값은 서버에만 두고 현재 설정값은 유지한다.
# 기능: Backend server-only 환경변수 이름 예시
PORT=3000
SUPABASE_URL=
SUPABASE_SECRET_KEY=
KAKAO_REST_API_KEY=
# 필요 시 공개데이터 API key를 서버 환경변수로 추가

# Anjeonhagil local routing worker (서버 전용)
ROUTING_WORKER_URL=http://127.0.0.1:8100
ROUTING_WORKER_TOKEN=
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
