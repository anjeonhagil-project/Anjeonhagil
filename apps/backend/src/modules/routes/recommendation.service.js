// 구현 예정(Anjeonhagil): 검증·중복 병합·Q3 필터가 끝난 후보를 공통 모델로 정렬하고 추천 후보와 근거를 반환한다.
// 입력: 서버의 후보 목록, 고정 profile/model snapshot. 출력: 표시 순서, 추천 candidate_id, 계산값에 근거한 설명.
// 모델 호출은 modules/models로 위임한다. 모델 미준비/실패 때는 최신 worker의 설문 기준 후보 정책을 사용한다.
// learned 프로필의 모델이 실패하면 설문 프로필로 복귀한 뒤 필요한 후보를 다시 계산한다. 다른 가중치의 경로를 설문 결과로 표시하지 않는다.
// 6개 부담의 서로 다른 단위를 임의로 더해 새 점수를 만들지 않는다. 안전성 보장이나 사고 확률로 설명하지 않는다.
// 추천 표시와 실제 노출·최종 선택은 별도 사건이다. 여기에서 사용자 선택 이벤트를 자동 생성하지 않는다.
