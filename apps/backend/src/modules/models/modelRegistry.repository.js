// 구현 예정(Anjeonhagil): ag_model_versions에서 활성 모델과 과거 snapshot의 정확한 모델 버전을 조회한다.
// 역할: 모델 종류, 계약/피처/ETA/scale 버전, scaler, artifact 경로·해시, 학습 manifest 해시를 함께 반환한다.
// 활성 모델 조회와 지정 버전 조회를 구분하고, 과거 검색에 현재 모델을 소급 적용하지 않는다.
// 등록된 버전의 내용은 불변이다. 학습 결과는 새 model_version으로 등록하고 활성 모델은 최대 하나로 유지한다.
// 모델 아티팩트가 없으면 survey_only_v1 상태를 사용한다. 빈 모델 파일이나 임시 학습 성능을 등록하지 않는다.
// DB 접근은 서버 service_role에 한정하며 아티팩트 경로·비밀값을 화면 응답에 그대로 내보내지 않는다.
