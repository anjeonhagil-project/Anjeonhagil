// 구현 예정(Anjeonhagil): Q4에 사용할 서울 OD 사례와 비교 횟수를 서버에서 관리한다. 아직 사례 데이터나 실행 코드는 없다.
// 역할: 기획의 A/B 2~3회 비교에 사용할 caseSetVersion, caseId, OD, 출발시각, 허용시간을 한곳에 정의한다.
// 사례는 최신 계산기로 검증한 서로 다른 두 경로가 있어야 한다. 중복 경로·영역 밖 OD·계산 실패를 응답으로 만들지 않는다.
// Q3가 null이면 이번 비교에 쓸 허용시간을 사용자에게 받아야 하며 임의 기본값으로 바꾸지 않는다.
// 사례 좌표·피처·정답은 브라우저 입력이나 임의 수치로 만들지 않는다. 사람의 선택 전에는 학습 정답이 없다.
// 연결: preferences/onboarding.service.js. 사례 집합과 필수 횟수를 함께 고정해 중단 후 같은 단계로 재개한다.
